apiVersion: v1
kind: Service
metadata:
  name: {{ include "wisework-pdpa.fullname" . }}
  labels:
    {{- include "wisework-pdpa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wisework-pdpa.selectorLabels" . | nindent 4 }}
