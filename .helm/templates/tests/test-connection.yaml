apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wisework-pdpa.fullname" . }}-test-connection"
  labels:
    {{- include "wisework-pdpa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wisework-pdpa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
